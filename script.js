const { createApp} = Vue
createApp({
    data() {
      return {
        message: 'Prova vue! :D',
        nato: 'because it does not work?!',
        immage: 'js.jpg',
      }
    }
  }).mount('#app')

